close-bureau
.python-version
.worktrees/
*.pyc
.serena/
.vscode/
.npm-cache/
bureau.egg-info/
.coverage

# Ignore machine-specific files that are generated (templates are source of truth)
**/generated

# Local configuration overrides (not committed)
local.yml

# Operational state and trash (machine-specific)
.archives/

# MCP server clones (shared across worktrees)
.mcp-servers/